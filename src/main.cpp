/**
 * launches the Pathfinder
 */

#include "appWindow.hpp"

/* === main function ========================== */
int main() {
    AppWindow appWindow(500, 500);
    appWindow.launchWin();
    return 0;
}
